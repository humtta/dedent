# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: "3"

tasks:
  test: go test ./...
  bench: go test -bench . -benchmem
  fmt: go fmt ./...
  vet: go vet ./...
  check: task fmt vet
